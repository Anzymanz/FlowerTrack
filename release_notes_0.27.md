## Highlights
- Refined tracker UI with a collapsible stock drawer, mix shortcuts toggles, and more reliable ROA/CBD handling.
- Overhauled stats window layout with content-based sizing and copy-to-clipboard output.
- Scraper settings improvements: auto-open export option, cleaner sizing behavior, and dark disabled scrollbars.
- Export/history reliability upgrades plus a live pagination indicator during captures.
- Build/config/test maintenance to keep frozen builds and settings migrations solid.

## Changes
- Tracker UI polish: ROA hide now drops THC/CBD columns consistently, preserves user column widths, and keeps CBD tracking state stable on startup.
- Added toggleable Mix stock/Mixed dose shortcuts and a persisted collapsible stock entry drawer.
- Stats window improvements: right-aligned values, content-based sizing, copy-to-clipboard action, and tighter spacing.
- Scraper settings updates: auto-open export option, improved sizing/height targets, dark disabled scrollbar, and cleaner theme application without warnings.
- Export/history tweaks: large export size warning and robust embedded history JSON fallback.
- Build/config/test maintenance: ensure mix utils bundled in frozen builds and expand tracker config/test coverage.
